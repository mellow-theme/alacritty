# Mellow for Alacritty
## Install Instructions
Download `mellow.toml` and put it somewhere accessible. Then in your
`alacritty.toml` add the following:
```toml
[general]
import = [
    "path/to/mellow.toml"
]
```
